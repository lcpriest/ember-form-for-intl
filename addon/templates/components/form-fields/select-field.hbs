{{#form-field propertyName
    object=object
    form=form
    control="form-controls/select"
    update=(action update) as |f|}}
  {{f.label}}
  {{f.control
      accesskey=accesskey
      autofocus=autofocus
      dir=dir
      disabled=disabled
      hidden=hidden
      lang=lang
      multiple=multiple
      options=options
      required=required
  }}
{{/form-field}}
